# If a user is not logged in and you set the bot to require logged in users, only the unverified permissions will do anything.
# if a user has a -permission.node, they will be UNABLE to use that command.
# The '*' node grants that user ALL permissions.

# Permissions granted to all users.
default:
    - command.help
    - command.ping
    - command.uptime
    - chat.urls
    - command.git
    - command.haspaid
    - command.wtc
    - command.imgur
    - command.devoice
    - command.resolve
    - command.geoip
    - command.mcstatus
    - command.calc
# Permissions granted to specific users.
TheReverend403:
# revthefox.co.uk: <-- if using hostmask matching.
    - example.permission
    - '*'